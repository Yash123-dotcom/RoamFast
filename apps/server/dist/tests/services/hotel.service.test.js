// Test disabled during Firebase migration
import { describe, it, expect } from 'vitest';
describe('HotelService', () => {
    it('should be disabled', () => {
        expect(true).toBe(true);
    });
});
